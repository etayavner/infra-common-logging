describe('Purchase Options Main Tests Suite', function () {

    beforeEach(function () {
        nock.cleanAll(); // Cleanup all the prepared mocks

    });

    afterEach(function () {
        nock.isDone(); // Verify all mocks expectations have been met
    });


    // Require tests for Subscriptions
    describe('infra-logging-common', function() {

    });
});